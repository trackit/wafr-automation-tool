# di-container

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build di-container` to build the library.

## Running unit tests

Run `nx test di-container` to execute the unit tests via [Vitest](https://vitest.dev/).
